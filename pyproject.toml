[project]
name = "python-4-blockheads"
version = "0.1.0"
description = "This is a project to teach Python with activities, teory and projects"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []
